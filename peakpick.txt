ALL PEAK INTENSITIES EXPORTED AS HEIGHTS

nhsqc peak-pick parameters:
 - spectrum contour levels:
   * global scale:  2500
   * base level:  0.00025
 - find parameters:
   * extrema to search for: positive only
   * nearby points to check: +-1 in at most one dim
   * scale relative: 5.0000
   * exclusion buffer: 2
   * extra thickness: 0
   * minimum drop factor: 0
 - region peak find:
   * dim1:  ~10.7 - ~ 6.3 or maybe entire region (not sure exactly)
   * dim2:  entire region (not sure)

hnco peak-pick parameters:
 - spectrum contour levels:
   * global scale:  2500
   * base level:  0.00025
 - find parameters:
   * extrema to search for: positive only
   * nearby points to check: +-1 in at most one dim
   * scale relative: 2.0000
   * exclusion buffer: 2
   * extra thickness: 0
   * minimum drop factor: 0
 - region peak find:
   * dim1:  170.542 - 181.501 (entire region)
   * dim2:  6.600   - 10.700
   * dim3:  99.683  - 130.449 (entire region)

hncacb peak-pick parameters:
 - spectrum contour levels:
   * global scale:  2500
   * base level:  0.00025
 - find parameters:
   * extrema to search for: positive and negative
   * nearby points to check: +-1 in at most one dim
   * scale relative: 2.5000
   * exclusion buffer: 1
   * extra thickness: 0
   * minimum drop factor: 0
 - region peak find:
   * dim1:  entire region (?)
   * dim2:  entire region (?)
   * dim3:  entire region (?)

c(co)nh-tocsy peak-pick parameters:
 - spectrum contour levels:
   * global scale:  2500
   * base level:  0.00057643
 - find parameters:
   * extrema to search for:  positive only
   * nearby points to check: +-1 in at most one dim
   * scale relative: 2.5000
   * exclusion butter: 1
   * extra thickness: 0
   * minimum drop factor: 0
 - region peak find:
   * dim1: entire region
   * dim2: entire region
   * dim3: entire region

hc(co)nh-tocsy peak-pick parameters:
 - spectrum contour levels:
   * global scale: 2500
   * base level: 0.00029315
 - find parameters:
   * extrema to search for:  positive only
   * nearby points to check: +-1 in at most one dim
   * scale relative: 3.5000
   * exclusion buffer: 1
   * extra thickness: 0
   * minimum drop factor: 0
 - region peak find:
   * dim1: -0.992 to 10.581
   * dim2: 5.787 to 12.332
   * dim3: 99.683 to 130.450

hbha(co)nh peak-pick parameters:
 - spectrum contour levels:
   * global scale: 2500
   * base level: 0.00040
 - find parameters:
   * extrema to search for:  positive and negative
   * nearby points to check: +-1 in at most one dim
   * scale relative: 3.5000
   * exclusion buffer: 1
   * extra thickness: 0
   * minimum drop factor: 0
 - region peak find:
   * dim1: -0.196 to 9.780
   * dim2: 6.50 to 11.0
   * dim3: 99.804 to 130.449

hbCBcgcdceHE peak-pick parameters:
 - spectrum contour levels:
   * global scale: 2500
   * base level: 0.00070
 - find parameters:
   * extrema to search for:  positive only
   * nearby points to check: +-1 in at most one dim
   * scale relative: 2.000
   * exclusion buffer: 1
   * extra thickness: 0
   * minimum drop factor: 0
 - region peak find:
   * dim1: 6.4 to 7.6
   * dim2: 26.0 to 46.0

hbCBcgcdHD peak-pick parameters:
 - spectrum contour levels:
   * global scale: 2500
   * base level: 0.00070
 - find parameters:
   * extrema to search for:  positive only
   * nearby points to check: +-1 in at most one dim
   * scale relative: 2.000
   * exclusion buffer: 1
   * extra thickness: 0
   * minimum drop factor: 0
 - region peak find:
   * dim1: 6.20 to 7.60
   * dim2: 26.0 to 46.0
